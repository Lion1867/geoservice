# GeoAI — Геоаналитическая система с RAG-микросервисом

> Интеллектуальная система для анализа градостроительной документации с применением технологий Retrieval-Augmented Generation (RAG) и YandexGPT. Система отвечает на вопросы пользователей, опираясь на актуальные нормативно-правовые акты РФ в области строительства и землепользования.

---

## Содержание

- [Обзор архитектуры](#обзор-архитектуры)
- [Структура проекта](#структура-проекта)
- [Микросервисы](#микросервисы)
  - [Основной бэкенд — backend/main.py](#основной-бэкенд--backendmainpy)
  - [RAG-микросервис — backend/rag_core_final/rag_server.py](#rag-микросервис--backendrag_core_finalrag_serverpy)
- [Технологический стек](#технологический-стек)
- [База знаний (датасет)](#база-знаний-датасет)
- [Требования к окружению](#требования-к-окружению)
- [Установка и запуск](#установка-и-запуск)
- [Переменные окружения](#переменные-окружения)
- [API-эндпоинты](#api-эндпоинты)
- [Фронтенд](#фронтенд)
- [Этические и правовые аспекты](#этические-и-правовые-аспекты)

---

## Обзор архитектуры

Система построена по микросервисной архитектуре и включает пять ключевых компонентов:

```
┌─────────────────────────────────────────────────────────┐
│                    Клиент (Frontend)                     │
└───────────────────────────┬─────────────────────────────┘
                            │ HTTP
┌───────────────────────────▼─────────────────────────────┐
│              Основной бэкенд (main.py)                   │
│   Оркестратор: гео-аналитика + кадастр + RAG-запросы    │
└────────────┬──────────────────────────────┬─────────────┘
             │ HTTP                         │ HTTP
┌────────────▼──────────────┐  ┌────────────▼─────────────┐
│    Гео/кадастровые        │  │   RAG-микросервис         │
│    внешние API            │  │   (rag_server.py)         │
│    (Яндекс Карты и др.)   │  │   FastAPI + Qdrant        │
└───────────────────────────┘  └────────────┬─────────────┘
                                            │
                               ┌────────────▼─────────────┐
                               │    Yandex Embedder        │
                               │    + Qdrant (векторная БД)│
                               └────────────┬─────────────┘
                                            │
                               ┌────────────▼─────────────┐
                               │    YandexGPT              │
                               │    (генерация ответа)     │
                               └──────────────────────────┘
```

**Поток обработки запроса:**

1. Пользователь отправляет вопрос через интерфейс.
2. Основной бэкенд параллельно собирает данные через гео-аналитический и кадастровый модули.
3. Если вопрос касается строительных норм — запрос перенаправляется в RAG-микросервис.
4. RAG-микросервис через Yandex Embedder создаёт вектор запроса и выполняет семантический поиск в Qdrant.
5. Найденные нормативные фрагменты возвращаются оркестратору.
6. Вся собранная информация передаётся в YandexGPT для генерации финального ответа.
7. Ответ возвращается пользователю на клиентскую часть.

---

## Структура проекта

```
geoservice/
│
├── backend/                          # Весь серверный код
│   │
│   ├── main.py                       # 🚀 Микросервис 1: Основной бэкенд (оркестратор)
│   │
│   ├── rag_core_final/               # 🚀 Микросервис 2: RAG-система
│   │   ├── rag_server.py             # FastAPI-сервер RAG-микросервиса
│   │   ├── rag_pipeline.py           # Логика индексации и поиска
│   │   ├── embedder.py               # Обёртка над Yandex Embedder
│   │   ├── vector_store.py           # Клиент Qdrant
│   │   └── chunker.py                # Разбиение документов на чанки
│   │
│   ├── geo/                          # Геоаналитический модуль
│   │   ├── geo_client.py             # Клиент геосервисов (Яндекс Карты и др.)
│   │   └── cadastre_client.py        # Кадастровый модуль
│   │
│   ├── data/                         # Нормативные документы (датасет)
│   │   ├── raw/                      # Исходные PDF-файлы
│   │   └── processed/                # Обработанный текст (TXT-чанки)
│   │
│   ├── notebooks/                    # Jupyter-ноутбуки для подготовки данных
│   │   ├── data_collection.ipynb     # Сбор и извлечение текста из PDF
│   │   ├── chunking.ipynb            # Разбиение на чанки и векторизация
│   │   └── stats.ipynb               # Описательная статистика датасета
│   │
│   ├── requirements.txt              # Зависимости Python
│   └── .env.example                  # Шаблон переменных окружения
│
├── frontend/                         # Клиентская часть
│   ├── index.html                    # Главная страница
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── app.js                    # Логика взаимодействия с API
│
├── .gitignore
└── README.md
```

---

## Микросервисы

Система запускается двумя независимыми процессами. Каждый — отдельный FastAPI-сервер на своём порту.

---

### Основной бэкенд — `backend/main.py`

**Роль:** Оркестратор. Принимает запросы от фронтенда, агрегирует данные из геомодулей и RAG-сервиса, передаёт итоговый контекст в YandexGPT и возвращает ответ.

**Порт по умолчанию:** `8000`

**Основные функции:**

- Приём пользовательских запросов через REST API.
- Параллельный сбор геоаналитической и кадастровой информации.
- Маршрутизация нормативных вопросов в RAG-микросервис (порт `8001`).
- Агрегация всех данных и формирование промпта для YandexGPT.
- Возврат структурированного ответа с источниками.

**Запуск:**

```bash
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

---

### RAG-микросервис — `backend/rag_core_final/rag_server.py`

**Роль:** Специализированный сервис семантического поиска по нормативно-правовой базе знаний. Принимает текстовый запрос, ищет релевантные фрагменты в Qdrant и возвращает топ-N результатов.

**Порт по умолчанию:** `8001`

**Пайплайн работы:**

```
Запрос (текст)
    │
    ▼
Yandex Embedder
(текст → вектор 1536 dims)
    │
    ▼
Qdrant — косинусный поиск
(топ-5 ближайших чанков)
    │
    ▼
Возврат фрагментов + метаданных
(источник, номер статьи, релевантность)
```

**Параметры чанкинга:**

| Параметр | Значение |
|---|---|
| Метод | `RecursiveCharacterTextSplitter` (LangChain) |
| Размер чанка | 500 токенов |
| Перекрытие (overlap) | 10% (50 токенов) |
| Размерность вектора | 1 536 |

**Запуск:**

```bash
cd backend/rag_core_final
uvicorn rag_server:app --host 0.0.0.0 --port 8001 --reload
```

---

## Технологический стек

### Бэкенд

| Компонент | Технология | Назначение |
|---|---|---|
| Веб-фреймворк | FastAPI | REST API для обоих микросервисов |
| ASGI-сервер | Uvicorn | Запуск FastAPI-приложений |
| LLM | YandexGPT | Генерация финального ответа |
| Эмбеддинги | Yandex Embedder (`text-embeddings`) | Векторизация текста, 1536 dims |
| Векторная БД | Qdrant | Хранение и поиск эмбеддингов |
| RAG-фреймворк | LangChain | Чанкинг, оркестрация RAG-пайплайна |
| Реляционная БД | SQLite | Хранение истории диалогов, пользователей |
| PDF-парсинг | pdfplumber, pypdf | Извлечение текста из нормативных документов |
| NLP-обработка | NLTK | Токенизация, лемматизация, стоп-слова |
| HTTP-клиент | httpx / aiohttp | Асинхронные запросы к внешним API |

### Фронтенд

| Компонент | Технология |
|---|---|
| Разметка | HTML5 |
| Стили | CSS3 |
| Логика | JavaScript (Vanilla) |

### Инфраструктура

| Компонент | Технология |
|---|---|
| Контроль версий | Git / GitHub |
| Ноутбуки (data science) | Jupyter Notebook |
| Форматы данных | PDF (исходники) → TXT (обработанные чанки) |

---

## База знаний (датасет)

RAG-микросервис работает на корпусе нормативно-правовых актов РФ в области градостроительства.

### Состав документов

| # | Документ |
|---|---|
| 1 | Градостроительный кодекс РФ |
| 2 | Земельный кодекс РФ |
| 3 | СНиП (актуализированные редакции — Своды правил, СП) |
| 4 | СанПиН — санитарные правила и нормы |
| 5 | ГОСТ в области строительства |
| 6 | Правила землепользования и застройки (ПЗЗ) |
| 7 | СП 4.13130 (Системы противопожарной защиты) и др. |

### Источники

- [pravo.gov.ru](https://pravo.gov.ru) — Официальный интернет-портал правовой информации
- [docs.cntd.ru](https://docs.cntd.ru) — Электронный фонд правовых и нормативно-технических документов
- [minstroyrf.gov.ru](https://minstroyrf.gov.ru) — Сайт Минстроя России

### Статистика датасета

| Показатель | Значение |
|---|---|
| Общий объём | ~10 МБ / ~3 000 000 символов |
| Количество документов | ~15 ключевых нормативных актов |
| Всего предложений | 14 000+ |
| Всего слов | ~302 000 |
| Уникальных слов | 26 837 |
| TTR (лексическое разнообразие) | 0.0889 |
| Средняя длина слова | 6.90 символов |
| Средняя длина предложения | 21.58 слов |
| Размерность эмбеддинга | 1 536 |

### Подготовка данных

Документы обрабатываются в несколько этапов:

```
PDF-документы
    │
    ▼  pdfplumber / pypdf
Извлечение текстового слоя (TXT)
    │
    ▼  NLTK
NLP-предобработка:
  - токенизация
  - лемматизация
  - удаление стоп-слов
    │
    ▼  LangChain RecursiveCharacterTextSplitter
Чанкинг (500 токенов, overlap 10%)
    │
    ▼  Yandex Embedder
Векторизация каждого чанка (1536 dims)
    │
    ▼  Qdrant
Сохранение в векторную базу данных
```

---

## Требования к окружению

- Python **3.10+**
- Запущенный экземпляр **Qdrant** (локально или облачный)
- Доступ к **Yandex Cloud API** (YandexGPT + Yandex Embedder)
- (Опционально) доступ к геосервисным API (Яндекс Карты и др.)

---

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/Lion1867/geoservice.git
cd geoservice
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Linux / macOS
source venv/bin/activate

# Windows
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r backend/requirements.txt
```

### 4. Настройка переменных окружения

```bash
cp backend/.env.example backend/.env
# Заполните backend/.env своими ключами (см. раздел ниже)
```

### 5. Запуск Qdrant

Самый простой способ — через Docker:

```bash
docker run -d \
  --name qdrant \
  -p 6333:6333 \
  -v $(pwd)/qdrant_storage:/qdrant/storage \
  qdrant/qdrant
```

### 6. Индексация базы знаний

Перед первым запуском необходимо заполнить векторную базу данных:

```bash
cd backend
python -m rag_core_final.rag_pipeline --index
```

Или запустите соответствующий Jupyter-ноутбук:

```bash
jupyter notebook backend/notebooks/chunking.ipynb
```

### 7. Запуск микросервисов

Оба микросервиса запускаются **в отдельных терминалах**:

**Терминал 1 — RAG-сервис (запускать первым):**

```bash
cd backend/rag_core_final
uvicorn rag_server:app --host 0.0.0.0 --port 8001 --reload
```

**Терминал 2 — Основной бэкенд:**

```bash
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 8. Запуск фронтенда

Откройте `frontend/index.html` в браузере или запустите простой HTTP-сервер:

```bash
cd frontend
python -m http.server 3000
# Откройте http://localhost:3000
```

### Проверка работоспособности

```bash
# Основной бэкенд
curl http://localhost:8000/health

# RAG-микросервис
curl http://localhost:8001/health
```

---

## Переменные окружения

Создайте файл `backend/.env` на основе `.env.example`:

```env
# ─── Yandex Cloud ───────────────────────────────────────
YANDEX_API_KEY=your_yandex_api_key
YANDEX_FOLDER_ID=your_folder_id

# YandexGPT
YANDEXGPT_MODEL_URI=gpt://your_folder_id/yandexgpt/latest

# Yandex Embedder
YANDEX_EMBEDDER_URI=emb://your_folder_id/text-search-doc/latest
YANDEX_EMBEDDER_QUERY_URI=emb://your_folder_id/text-search-query/latest

# ─── Qdrant ─────────────────────────────────────────────
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=normative_docs

# ─── RAG-сервис ─────────────────────────────────────────
RAG_SERVICE_URL=http://localhost:8001
RAG_TOP_K=5                    # Количество возвращаемых фрагментов

# ─── Основной бэкенд ────────────────────────────────────
MAIN_BACKEND_HOST=0.0.0.0
MAIN_BACKEND_PORT=8000

# ─── Геосервисы (опционально) ───────────────────────────
YANDEX_MAPS_API_KEY=your_maps_api_key
CADASTRE_API_URL=https://pkk.rosreestr.ru/api

# ─── SQLite ─────────────────────────────────────────────
DATABASE_URL=sqlite:///./geoai.db

# ─── Разработка ─────────────────────────────────────────
DEBUG=true
LOG_LEVEL=INFO
```

---

## API-эндпоинты

### Основной бэкенд (`http://localhost:8000`)

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/health` | Проверка состояния сервиса |
| `POST` | `/api/ask` | Задать вопрос системе (основной эндпоинт) |
| `GET` | `/api/history` | История диалога текущей сессии |
| `POST` | `/api/geo/analyze` | Геоаналитика по координатам или адресу |
| `GET` | `/api/cadastre/{cadastre_number}` | Получить кадастровые данные объекта |

#### `POST /api/ask` — пример запроса

```json
{
  "question": "Какое минимальное расстояние от жилого дома до красной линии улицы по СНиП?",
  "session_id": "user-session-abc123"
}
```

#### `POST /api/ask` — пример ответа

```json
{
  "answer": "Согласно СП 42.13330.2016 (актуализированная редакция СНиП 2.07.01-89), расстояние от стен жилых домов до красных линий улиц и проездов должно быть не менее 5 метров...",
  "sources": [
    {
      "document": "СП 42.13330.2016",
      "chunk": "...расстояние от стен жилых домов до красных линий...",
      "score": 0.94
    }
  ],
  "session_id": "user-session-abc123"
}
```

---

### RAG-микросервис (`http://localhost:8001`)

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/health` | Проверка состояния и доступности Qdrant |
| `POST` | `/search` | Семантический поиск по базе знаний |
| `POST` | `/index` | Переиндексация коллекции документов |
| `GET` | `/collection/info` | Информация о коллекции Qdrant |

#### `POST /search` — пример запроса

```json
{
  "query": "нормы инсоляции жилых помещений",
  "top_k": 5
}
```

#### `POST /search` — пример ответа

```json
{
  "results": [
    {
      "text": "Продолжительность непрерывной инсоляции жилых помещений должна составлять не менее 2,5 часа в день...",
      "source": "СанПиН 1.2.3685-21",
      "score": 0.91,
      "chunk_id": "sanpin_1_2_3685_21_chunk_047"
    }
  ],
  "query_vector_dims": 1536,
  "search_time_ms": 12
}
```

---

## Фронтенд

Клиентская часть представляет собой веб-интерфейс на HTML/CSS/JS, взаимодействующий с основным бэкендом по REST.

**Возможности:**

- Чат-интерфейс для общения с системой.
- Отображение источников нормативных документов под каждым ответом.
- Интерактивная карта (Яндекс Карты) для выбора геолокации.
- История диалога в рамках сессии.

---

## Этические и правовые аспекты

Все нормативные документы, используемые в базе знаний, являются официальными документами государственных органов РФ. Согласно ст. 1259 ГК РФ, официальные документы государственных органов не являются объектами авторского права, что делает их использование в системе полностью легитимным.

Система является **инструментом поддержки принятия решений**. Результаты анализа носят информационно-справочный характер и **не заменяют официальной градостроительной экспертизы** или консультации сертифицированного юриста.

Архитектура RAG обеспечивает привязку ответов к конкретным пунктам нормативных документов, что минимизирует риск «галлюцинаций» языковой модели.

---

## Авторы

**Иванов Илья** — проектирование архитектуры, сбор и подготовка датасета, разработка RAG-пайплайна.

---

*Репозиторий: [https://github.com/Lion1867/geoservice](https://github.com/Lion1867/geoservice)*
